export default (theme) => ({
    authBox: {
        width: '100%',
        background: theme.palette.primary.main,
        marginTop: '1%',
        padding: '1%',
        borderRadius: '15px'
    }
});
